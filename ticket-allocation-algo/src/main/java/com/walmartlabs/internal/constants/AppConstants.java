package com.walmartlabs.internal.constants;

public class AppConstants {
	
	/* Default no of rows to be used if external/local configs are not available*/
	public static final int DEFAULT_ROWS = 10;
	
	/* Default no of columns to be used if external/local configs are not available*/
	public static final int DEFAULT_COLUMNS = 20;
	
	/* Default application launchMode to be used if external/local configs are not available*/
	public static final String DEFAULT_LAUNCH_MODE = "optimized";
}
